# CLAUDE.md

**STOP. Before you do anything in this repo, open and read `HOUSE-RULES.md` in full.**

`HOUSE-RULES.md` is the binding source of truth for Keepstead™ — the stack, the seven
rooms, the Keeper pricing ladder, the themes, the migration map, and the
non-negotiable build rules. You read it every session, no exceptions.

The three standing rules, in short:
1. **Read before you build** — search for an existing file/component/route/RPC first; extend, don't duplicate.
2. **Named surfaces only** — build on the exact file/route/table the spec names. No parallel variants.
3. **Report what you touched** — every deliverable ends with a list of every file created or modified.

Now go read `HOUSE-RULES.md`.
